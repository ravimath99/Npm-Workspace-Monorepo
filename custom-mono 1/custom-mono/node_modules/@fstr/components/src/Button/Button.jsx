import React from "react"
import "./Button.css";

export const Button = ({ text = "" }) => {
    return (
        <button type="button" className="btn">
            {text} button jsx old
        </button>
    )
}